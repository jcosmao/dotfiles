# tmux functions
function _complete_tmux_session
{
    COMPREPLY=()
    local session_name="${COMP_WORDS[COMP_CWORD]}"
    COMPREPLY=( ls $(compgen -W "$(tm ls)" -- "$session_name") $( tm --help | grep -Eo '\-\-\w+' | sort -u | xargs))
}

complete -F _complete_tmux_session tm
